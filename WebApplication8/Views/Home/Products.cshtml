@{
    ViewData["Title"] = "Products (CRUD)";
}

<h1>@ViewData["Title"]</h1>
<p class="text-muted">
    Full Create / Read / Update / Delete against <code>/api/products</code>.
    Every action calls the ASP.NET API and re-loads the list.
</p>

<!-- React mounts here -->
<div id="crud-root"></div>

@section Scripts {
    <script type="module" src="~/dist/crud-demo.js" asp-append-version="true"></script>
}
